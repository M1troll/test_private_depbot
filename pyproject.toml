[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
python = "^3.12"
pytest = "^8.2.2"
pomcorn = "^0.8.0"

private_repo = { git = "ssh://git@github.com/M1troll/private_repo.git", rev = "develop" }
